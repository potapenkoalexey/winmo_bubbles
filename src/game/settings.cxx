#include "settings.hxx"

int settings::initialize(grottans::engine* engine, const std::string_view& file)
{
    //    std::stringstream buf = engine->load_txt_and_filter_comments(file);
    //    char* temp;

    //    buf.getline(temp, 80, ' ');

    //    return 0;
}
